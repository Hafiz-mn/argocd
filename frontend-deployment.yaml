apiVersion: apps/v1
kind: Deployment
metadata:
 name: frontend
 labels:
   app: Frontend
spec:
 replicas: 1
 selector:
   matchLabels:
     app: Frontend
 template:
   metadata:
     labels:
       app: Frontend
   spec:
     containers:
     - name: frontend
       image: public.ecr.aws/w0g4t3k7/mypub:frontend
       ports:
       - containerPort: 80












